[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3.11 -m pip install -r api/requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn api.index:app --host 0.0.0.0 --port $PORT --workers 2"
